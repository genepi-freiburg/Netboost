# C++11 required.
CXX_STD=CXX11

AVX=-mavx

# AVX flag will be added for Linux/MacOS (and therefore GCC/Clang) by configure.
PKG_CXXFLAGS=`${R_HOME}/bin/Rscript -e "Rcpp:::CxxFlags()"` ${AVX}
# /arch:AVX

# Strip debugging symbols (should work GCC/Clang)
PKG_LIBS=`${R_HOME}/bin/Rscript -e "Rcpp:::LdFlags()"` -Wl,--strip-debug
