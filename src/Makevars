# C++11 required.
CXX_STD = CXX11

# Also, AVX(1) is mandatory (and no portable R definition found)
PKG_CXXFLAGS=`${R_HOME}/bin/Rscript -e "Rcpp:::CxxFlags()"` -mavx

PKG_LIBS=`${R_HOME}/bin/Rscript -e "Rcpp:::LdFlags()"`
